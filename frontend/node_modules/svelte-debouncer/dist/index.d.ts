export default class Debouncer {
    callback: Function;
    delay: number;
    debounce_first: boolean;
    timer: number;
    is_first_debounce: boolean;
    constructor(callback: Function, delay?: number, debounce_first?: boolean);
    debounce(...args: any[]): void;
}
